rules_version = '2';

// 方針: クライアントからの直接アクセスは全て禁止。
// 読み書きはすべて Nuxt サーバー(firebase-admin)経由で行い、
// 権限制御は server/utils/permissions.ts に集約する。
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
